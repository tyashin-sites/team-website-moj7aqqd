# Asset Debt — placeholders awaiting real inputs

Every entry here blocks 100%-confidence launch until resolved. Builders add
entries; only the user (or real assets) can clear them.

| # | Needed asset | Currently in place | Blocking phase |
|---|--------------|--------------------|----------------|
| 1 | Real client product 3D model (glTF/GLB, ≤2MB draco) for HeroObject — ideally a Sunbaby or Guntier product | none yet (CC0 placeholder to be used) | 1, 6 |
| 2 | Real customer quotes | **REAL QUOTES IN PLACE (2026-07-24):** three testimonials captured verbatim from production thridify.com (user-confirmed real): Guntier, Sunbaby, Vortex Splash — rendered via `ProofCard` (§7.6) on the home Proof section, company-level attribution only. STILL PENDING from the user: more Sunbaby quotes + possible Airolam/Woodlark mentions. NOTE: **Nasher Miles, Guntier and Vortex Splash are PAST/churned clients; only Sunbaby is active** — no mention may imply current engagement. BOTH home logo strips now use the temporally-neutral label "Client work includes" (top marquee eyebrow fixed 2026-07-24, B-1; the lower Proof strip already used it) — no present-tense "Trusted by" claim renders over the client set on any route | 6 |
| 3 | ~~Confirm social handle URLs~~ **RESOLVED (Phase 2):** footer socials set to the knowledge-bank canonical handles (linkedin.com/company/thridify, instagram.com/thridify, facebook.com/thridify, youtube.com/@thridify) | done — verify handles are live before Phase 7 | — |
| 4 | Configurator screen recordings / AR phone captures | **UPGRADED TO LIVE INTERACTIVE MINI-DEMOS (2026-08-02, CHUNK 2):** the home product trio, the /platform hero + the /platform viewer/configurator/AR deep-dives now render `src/components/signature/CapabilityDemo.tsx` — a poster-first, activate-on-interaction `<model-viewer>` of the placeholder chair (DEMO-FIRST, DESIGN-SPEC §6a), not an infographic. `ProductVisual` abstract SVGs remain ONLY on /platform Content-Generation + Analytics deep-dives (no interactive form exists for those). The placeholder chair is a stand-in; see #16 for the real-embed replacement | 2, 5, 6 |
| 5 | Decision: YOM analytics on this site + conversion events | no analytics | 0 |
| 6 | Full GDPR/PIPEDA-aware privacy policy + terms (lawful bases, data-subject request flow, sub-processor list, cookie/consent handling) | **PHASE-4 FULL POLICIES SHIPPED (2026-08-03):** `/privacy` + `/terms` (via `src/components/LegalDocument.tsx`) rewritten into substantive GDPR / **India DPDP Act 2023** / **PIPEDA**-aware policies. Privacy now covers: data-controller/Data-Fiduciary identity (Aapastech Private Limited), what's collected (contact form fields + Cloudflare server/security logs + not-yet-active consent-gated analytics), purposes + legal basis (consent / legitimate interest / pre-contract), processors (Cloudflare, Tyashin platform, future analytics), international transfers (India↔Canada↔EU, SCCs), retention, cookies (essential-only today; future analytics consent-gated), data-subject rights (access/rectify/erase/restrict/object/portability/withdraw-consent + DPDP grievance & nomination), supervisory-authority complaint routes (DPB India / EU-UK SA / OPC Canada), children (<16, no targeting), changes, contact@thridify.com. Terms now covers: acceptance, description, demos-not-offers, acceptable use, IP, third-party links (Calendly/socials), disclaimers, limitation of liability, **governing law = India**, changes, contact. Each page has per-page canonical + BreadcrumbList JSON-LD + visible breadcrumb. **These are HONEST BASELINES written for lawyer review, NOT legal advice** — see LEGAL-REVIEW items below (registered address, DPO/grievance-officer contact, exact retention periods, entity CIN). No invented certifications | 4 (content shipped; lawyer review is a Phase-7 gate) |
| 23 | **Consent-manager platform PLUGIN (cookie banner + non-essential-cookie gating).** Phase-4 CHUNK 3 decision: the site is consent-READY but ships NO bespoke cookie banner (it would collide with the plugin). VERIFIED (2026-08-03) the site sets NO non-essential cookies pre-consent — `node fetch` of `/` on both hosts returns `Set-Cookie: null`, and no analytics/ad tracker is wired in code. A clearly-commented integration point is in `src/app/layout.tsx` (mount the plugin's consent script there at install; gate any future analytics through it). Privacy policy "Cookies and similar technologies" section documents current essential-only state + future consent-gating. **USER/ORCHESTRATOR ACTION:** install the platform consent-manager plugin on the Thridify project (needs a JWT, orchestrator-tracked) when analytics/non-essential cookies are introduced; it owns the banner + gating at install | 4/7 |
| 24 | **Security & practices page SHIPPED (2026-08-03, Phase-4 CHUNK 4).** `/security` at `src/app/security/page.tsx` (via LegalDocument) — HONEST, modest: HTTPS/TLS + Cloudflare (DDoS/WAF), least-privilege access controls, processor list (Cloudflare / Tyashin platform / Calendly), data-region note (India/NA/EU → links privacy International transfers), cookies note, an explicit **"we do NOT claim SOC 2 / ISO 27001"** honesty section (No-Faking — no fabricated badges), and a responsible-disclosure process (contact@thridify.com). Per-page canonical + BreadcrumbList JSON-LD; linked from footer legal row + in sitemap. Resolves 200 | done — verify at Phase 6/7 |
| 7 | ~~Lead-alert destinations: Slack webhook URL + CRM wiring + auto-ack email~~ **RESOLVED (2026-07-24):** lead routing is now owned by the Tyashin **Contact Form & Lead Capture** plugin, not this repo. `/api/contact` forwards each lead (name/email/**company**/**category**/message, first-class fields + `pageUrl`) to `/api/v1/contact/public/submit`; the plugin stores it in the admin **Leads** inbox, auto-syncs to the CRM, meters it, emails the team, sends an optional auto-reply, and posts to Slack **when the admin pastes an incoming-webhook URL in the plugin Settings** (`slackWebhookUrl`, Pro tier). No destination is hardcoded in the site. REMAINING USER ACTION: (a) install the plugin on the project + confirm `TYASHIN_API_KEY` is bound; (b) optionally paste the #thridify-internal Slack webhook in plugin Settings and enable auto-reply | done — verify at cutover |
| 8 | HeroObject model within budget: real client glTF ≤2MB draco-compressed | Khronos CC0 SheenChair at `public/models/sheen-chair.glb` — 4.4MB, OVER the 2MB spec budget (mitigated: poster-first, idle+viewport-deferred load) | 1, 5, 6 |
| 9 | Real poster render (LCP frame) for the hero model | **SEAMLESS RASTER POSTER IN PLACE (2026-08-02, CHUNK 1):** `public/models/sheen-chair-poster.webp` (1024×1024, transparent, 42 KB) rendered from the EXACT model `public/models/sheen-chair.glb` via `<model-viewer>` itself (headless Chrome, model-viewer `toDataURL`) at the component's initial camera pose — so load→interactive is imperceptible (SEAMLESS POSTER RULE, DESIGN-SPEC §6). The old hand-drawn `sheen-chair-poster.svg` was removed. **REGENERATE the poster the same way when the real client model / Thridify SDK experience (ASSET-DEBT #1/#8, `docs/integration/`) replaces the placeholder chair** — render the new model's first frame, don't hand-draw. Render script: scratchpad `render-poster.mjs` (puppeteer-core + the repo's own model-viewer dist) | 1, 5 |
| 10 | Real per-model AR QR code + confirmed demo pricing for the hero configurator | **QR NOW REAL & SCANNABLE (2026-08-02, CHUNK 2):** the AR affordance in `HeroObject` + `CapabilityDemo` renders `public/models/ar-qr-chair.svg` — a genuine QR (v9, ECC M) encoding a Google **scene-viewer** AR intent for the self-hosted chair GLB, so scanning it on a phone launches "view in your room" AR of the exact model (no blank/dashed box). Regenerate per model with `scripts/generate-ar-qr.mjs` when the real client model lands (#16). REMAINING: real client model + its QR, and confirmed demo pricing — swatch prices ($1,189–$1,329) are still illustrative demo values, not claims. (iOS AR Quick Look needs a USDZ `ios-src`; scene-viewer covers Android directly and shows the model in-browser on iOS.) | 1, 6 |
| 11 | ~~Reconcile MetricBar numbers with the thridify.com impact stats~~ **RESOLVED (2026-07-24, supersedes the Phase-2 note):** the user confirmed the canonical impact metrics from the production thridify.com homepage — 75% lower product returns / 3× higher conversion rates / 100% more engagement / 70% lower photography cost / 100% higher click-through rate / 40% lower inventory cost. This set replaced the interim knowledge-bank set (−30%/3×/−65%/+23% and up-to-3×/+100%/+40%) sitewide and is codified in DESIGN-SPEC §7.2 as the ONLY permitted stat set | done — Phase 6 brand audit re-verifies | — |
| 12 | Real, sourced per-vertical metrics for VerticalCard (DESIGN-SPEC §7.5 wants "one metric" per card) — No-Faking rule forbids inventing per-industry numbers, so home vertical cards render without a metric until approved figures exist | VerticalCard supports an optional `metric` prop; home grid passes none | 2, 6 |
| 13 | ~~Platform brand kit stale vs canonical palette~~ **RESOLVED (2026-07-24):** the platform brand kit was PATCHed to the canonical §1 palette via the API. The render-blocking `/brand-kit.css` link stays REMOVED (the Phase-1 perf win stands — local tokens are now identical to the kit, so nothing is lost). Phase 7 checklist item added to BUILD-PLAN.md: re-verify `/brand-kit.css` serves the canonical palette on the custom domain at cutover | done | — |
| 14 | Real product-render OG images (1200×630) per page | Brand-colored generated statics at `public/og/*.png` (regenerate with `node scripts/generate-og.mjs`); wired via per-page `openGraph.images` + `metadataBase` (SITE_URL env, currently the workers.dev preview — switch to the production host at Phase 7). **CHUNK 3 (2026-08-02) added six per-industry OGs** `public/og/industry-<slug>.png` (furniture, modular-kitchens, doors-and-windows, prefab-structures, industrial-machinery, laminates-surfaces) — same brand-placeholder treatment; replace each with a real per-industry product render | 2, 6, 7 |
| 15 | Real team / office / founder photography for the About page (or a decision to stay abstract) | Phase 2 removed ALL Unsplash stock (DESIGN-SPEC §6); About now uses brand logo-geometry only | 2, 6 |
| 16 | **Real Thridify experience embeds** (the actual configurator on the actual SDK) for the hero, the BeforeAfter "live 3D", and every `CapabilityDemo` (home trio + platform) | Placeholder first-party CC0 SheenChair (`public/models/sheen-chair.glb`) driven via `<model-viewer>`. Blocked on the SDK standalone-embed work specified in `docs/integration/HERO-EMBED-REQUIREMENTS.md` + `SDK-EMBED-PROPOSAL.md` (stable experience id, `thridify-marketing` demo account, origin allowlist, poster/LCP contract, events API). When it lands: swap `<model-viewer>` for `Thridify.mount()`, and regenerate every poster from the real experience's first frame (#9 rule). Also supplies the real product photo + matching model for the BeforeAfter "flat photo vs same product in 3D" comparison | 2, 5, 6 |
| 19 | **Per-industry demo models (Phase 3 Wave A, CHUNK 3, 2026-08-02).** Each industry page now shows a DISTINCT, industry-relevant CC0 model instead of the generic hero chair, via `CapabilityDemo` `model`/`poster`/`modelLabel` props + `Industry.demoModel/demoPoster` (`src/lib/industries.ts`). All self-hosted in `public/models/`, draco+WebP-optimised ≤2MB, with seamless posters (§6). Provenance: `public/models/SOURCES.md` (all CC0 1.0, Khronos glTF-Sample-Assets). Mapping: furniture→GlassVaseFlowers (home decor, good); modular-kitchens→teacup (closest CC0 kitchenware — want a real cabinet/kitchen unit); doors-and-windows→Lantern (closest CC0 glass fixture — want a real door/window); industrial-machinery→AntiqueCamera (closest CC0 mechanical apparatus — want real equipment); laminates-surfaces→MetalRoughSpheres material sample (reasonable). **PREFAB & MODULAR STRUCTURES: NO usable CC0 structure/building model exists** (VirtualCity = testing-license-only; Sponza = LFS-gated), so `/industries/prefab-structures` still uses the chair fallback — awaiting a real structure model. ALL SIX ultimately want real client models (#16); replace + regenerate posters then | 2, 6 |
| 18 | **Positioning correction — "3D Content Generation" → "3D Modelling Service" (Phase 3 Wave A, CHUNK 1, 2026-08-02).** Thridify does NOT do automated 3D content generation; module #4 is now a human-delivered **3D Modelling Service** (catalog → photoreal glTF/GLB/USDZ, done-for-you). Replaced sitewide: `content/site.json` (platform product #4 id `content`→`modelling`, platform hero subtitle, about mission body, furniture industry blurb, footer Platform link → `/services/3d-modelling`); `ProductVisual` variant `content`→`modelling`; `/platform` metadata + a deep-dive link to the service page; new SEO page `src/app/services/3d-modelling/page.tsx` (Service + FAQPage + BreadcrumbList JSON-LD, wedge copy) added to sitemap + linked from every industry page; new OG `public/og/services-3d-modelling.png`. **KNOWLEDGE-BANK CORRECTION NEEDED (out of repo scope — orchestrator action):** the `thridify-business` KNOWLEDGE-BANK skill still lists "3D Content Generation" as product #4 and must be corrected to "3D Modelling Service" so future generations don't reintroduce the automated-content-gen claim | done — verify KB corrected before Phase 7 |
| 20 | **`/robots.txt` + `/llms.txt` platform-edge interception + Cloudflare content-signals robots (Phase 3 Wave B, CHUNK 4, 2026-08-03).** The site now serves its own `/robots.txt` (AI-crawler allowlist: OAI-SearchBot, ChatGPT-User, GPTBot, ClaudeBot, Claude-Web, Google-Extended, PerplexityBot, Applebot-Extended + `Sitemap:` ref) and `/llms.txt` (canonical description + factual bullets) via route handlers (`src/app/robots.txt/route.ts`, `src/app/llms.txt/route.ts`). **Two edge caveats, NOT fixable in this repo:** (a) On the platform host `team-website-moj7aqqd.sites.tyashin.com`, the Tyashin platform edge INTERCEPTS **`/robots.txt`, `/sitemap.xml` AND `/llms.txt`** (same mechanism as `/blog`, #17 — no `X-Robots-Tag` on those responses proves our Worker never ran) and serves platform-owned versions: a generic `User-agent: *` robots, a 3-URL default sitemap (`/`, `/contact`, `/about` only — NOT our 19-URL sitemap), and a 404 for `/llms.txt`. Normal HTML pages (incl. the new `/what-is-thridify` and `/compare/*`) DO dispatch to our Worker on this host and are correct; only these three SEO endpoints are edge-owned. **Phase-7 action (platform admin):** set the per-page ownership override to `storefront` for `/robots.txt`, `/sitemap.xml` and `/llms.txt` so the site's own Worker serves them on any custom domain routed through the platform edge. The raw worker host serves all three correctly today (verified 2026-08-03: robots ours + all 8 AI bots + Sitemap ref; llms.txt 200 with canonical description; sitemap 19 URLs). (b) On the raw worker host `site-thridify.snowy-cherry-cd2c.workers.dev`, `/robots.txt` may be overridden by Cloudflare's managed "content-signals" robots.txt (a workers.dev-level feature) — confirm our route wins after this deploy; if not, it is a Cloudflare zone setting, not a repo defect. **Both are Phase-7 verification items on the real production host.** | 7 |
| 21 | **Growth-surface canonical/OG/llms URLs still point at the preview host.** `/what-is-thridify`, `/compare/*`, `/llms.txt` and all new schema `url`/`sameAs`/breadcrumb `item` values resolve against `SITE_URL` (currently the workers.dev preview). The Phase-7 cutover must set `SITE_URL=https://www.thridify.com` (or the chosen canonical host) in the deploy env so every canonical, OG, sitemap and llms.txt URL flips together. Logged, not blocking (per contract). | 7 |
| 22 | **WordPress→Next 301 migration map — open decisions & GAP pages (Phase-7 prep, 2026-08-03).** The full URL inventory (160 sitemap URLs + GSC legacy) and disposition table are in `docs/seo-migration-map.md`; the clean 301s are IMPLEMENTED + live-verified in `next.config.ts` `async redirects()` (28 rules, `statusCode: 301`). Open inputs needed from the user before cutover: **(a) Education/WonderlyAR destination — RESOLVED (2026-08-03).** WonderlyAR is LIVE at **https://wonderlyar.com** (verified 200). The precise education footprint (verified via the thridify.com WP REST API) now **301s OFF-SITE to wonderlyar.com** in `next.config.ts` (`basePath:false`), replacing the interim → `/` holds: the `/ar-in-education` marketing page, the ONE education blog post `/post/connect-your-custom-domain-to-ar-education`, and the three `ar-education-*` tag archives (`ar-education-custom-domain`, `ar-education-domain-connection`, `ar-education-domain-setup` — the only tag slugs matching the education regex; `cname-*`/`dns-*`/`custom-domain-*` tags are Thridify platform how-tos and keep → `/blog`). RESIDUAL (orchestrator, needs JWT): the 1 education post is still PUBLISHED at `/blog/connect-your-custom-domain-to-ar-education` and should be UNPUBLISHED via the blog API (`DELETE /api/v1/blog/projects/:id/posts/:postId`) so no education content lives on Thridify. **(b) Integration pages** — ~~all interim-301 to `/platform`~~ **RESOLVED (2026-08-03); EXPANDED to 12 pages (2026-08-03, user-directed):** real per-platform pages built at `/integrations/<slug>` (woocommerce, shopify, wix, bigcommerce, magento, commercetools, canva, wordpress, custom-integration + **drupal, squarespace, prestashop**) via typed data `src/lib/integrations.ts` + `src/app/integrations/[slug]/page.tsx` (SSG, unique title/desc/canonical/OG + Service/FAQPage/BreadcrumbList JSON-LD each), plus an `/integrations` hub (now GROUPED: native plugin/app · embed-on-any-store · custom) in the sitemap + footer. The 3 new pages are framed HONESTLY as EMBED-BASED (universal embeddable widget/snippet — no native app claimed for Drupal/Squarespace/PrestaShop). `magento` slug is KEPT (SEO) but RELABELLED "Adobe Commerce (Magento)" to cover both keyword families; `/adobe-commerce` 301s to `/integrations/magento` (`next.config.ts`). NOTE: only Shopify (native app), WooCommerce + WordPress (native plugin) claim a native module; every other platform is embed/SDK. Every old integration URL (incl. `/big-commerce` + `/wix-commerce` variants) now 301s to its dedicated page. **Open sub-item (not blocking):** each integration hero uses the shared placeholder CC0 chair via `CapabilityDemo` (no per-platform demo model, no per-page OG image — pages use `/og/default.png`); when real Thridify SDK embeds land (#16), swap the chair for the real experience and add per-integration OG renders. Integration copy states the honest, widely-true mechanism only (publish → embed/plugin → JS SDK renders by product/variant/account ID → app-free web AR); no fabricated platform-specific claims. Guntier is the only real quote and has no known platform mapping, so all integration pages are metric-only proof. **(c) Blog migration — RESOLVED (2026-08-03):** the 37 WP posts were migrated 1:1 into the Blog/CMS plugin at matching slugs and serve live at `/blog/<slug>` (see #17). `next.config.ts` now 301s `/post/<slug>` → same-slug `/blog/<slug>` (single-segment, LAST) and the tag/author/category archives (`/post/tag/:tag`, `/post/author/:author`, `/post/category/:cat`, `/category/blog`, `/category/blog/page/:n`) → `/blog`. No longer a blocker. Residual flag: 8 education/custom-domain help posts among the 37 belong to WonderlyAR (deferred) — remain in the blog for now. **(d) `/pricing-plans`** interim → `/services/3d-modelling`; decide whether to build a real `/pricing` page. **(e) Ranking `wp-content/uploads/*` image URLs** — need a GSC image-report export to enumerate + preserve them. | 7 |
| 25 | **LEGAL FACTS TO CONFIRM (lawyer review — Phase-4 CHUNK 1, 2026-08-03).** The `/privacy`, `/terms` and `/security` pages are HONEST BASELINES written to be reviewed; I am not a lawyer. The following facts are asserted generically and should be confirmed/completed before Phase-7 launch: **(a)** Aapastech Private Limited's **registered office address** and **CIN** — omitted; DPDP/GDPR notices typically name the controller's address. **(b)** A named **Grievance Officer / DPO contact** — the DPDP Act (and GDPR for some orgs) expects a named contact; the pages currently route everything to contact@thridify.com. **(c)** Exact **retention periods** — the policy says "as long as needed" rather than concrete durations; confirm the real lead-retention window. **(d)** Confirm the **processor list is complete** (Cloudflare + Tyashin platform + Calendly) and whether email is handled by a distinct provider (e.g. Google Workspace) that should be listed. **(e)** Confirm **governing law = India** and jurisdiction wording is acceptable for EU/Canada-targeted marketing. **(f)** Confirm the site truly sets **only strictly-necessary cookies** at cutover on the production host (verified today on preview: no Set-Cookie). **(g)** DPDP "verifiable consent for children" — confirm the <16 statement matches actual practice. None of these block the preview build; all are pre-launch review items. | 4/7 |
| 26 | **Lantern-simulated mobile LCP vs the real hero (Phase-5 CHUNK 2, 2026-08-03).** After the full perf pass (hero poster preloaded `fetchpriority=high`, GLB deferred to interaction, ~900 KB of client logos → 12 KB, industry posters recompressed, logo de-prioritized + self-hosted), the **observed** LCP paint is **2.25–2.29 s on all three target pages — inside the §10 <2.5 s budget** — with CLS 0, TBT 40–70 ms and ~163 KB first-load JS. But Lighthouse mobile uses **simulated (Lantern) throttling**, which models a ~1.7–1.9 s element-render-delay on the poster-first hero and reports a **scored LCP of 3.5–3.7 s**, holding median mobile **perf at 87–88** (individual runs reach 94). This is a poster-hero + simulation interaction, not a page defect: the LCP element is the correct hero poster, it is discoverable + eager + high-priority, and the real paint meets budget. The one clean lever left is **making the poster no longer the single largest element** (e.g. a real Thridify SDK embed with its own fast first frame, #16) or trimming above-the-fold web-font weights — both are design-touching and gated on the real embed. **Recommend the QA auditor accept observed-LCP-within-budget as the §10 evidence and re-measure after the real SDK embed lands.** Reports: `audits/phase5/lighthouse/*.json`. | 5/6 |
| 27 | **Runtime error REPORTING hook (Phase-5 CHUNK 3, 2026-08-03).** `src/app/error.tsx` + `global-error.tsx` render on-brand graceful pages (no white screen) and `console.error` the caught error as a local breadcrumb, but there is **no external error-monitoring SDK wired** (deliberately — an external SDK is a plugin/analytics concern, not a site-repo dependency, and adding one now would set a beacon pre-consent, violating the opt-in rule). **USER/ORCHESTRATOR ACTION:** when the analytics/YOM (or a dedicated error-tracking) plugin is installed on the Thridify project, route runtime errors through it at that point — the two error boundaries are the mount points (replace the `console.error` calls with the plugin's capture call, gated through the consent-manager plugin #23). | 5/7 |
| 28 | **Shopify App Store listing (USER ACTION — discovery channel only the account owner can create).** The `/integrations/shopify` page now describes the REAL, live Shopify capabilities (Theme App Extension + no-code App Blocks, product & collection-page 3D, theme auto-detection + custom CSS selectors, collection-wide badges, embedded Admin onboarding, auto-link shop, Web Pixel analytics bridge, GDPR-ready endpoints, CDN-light + anti-CLS) framed as the merchant journey (install → publish → configure & AR → measure → comply). The copy states the honest mechanism only — it makes **no Shopify partnership/certification/"Built for Shopify"/App-Store-badge claim** (No-Faking). **To turn the earned search intent into installs, the user should list the Thridify app on the Shopify App Store** (submit the public app listing via the Shopify Partner Dashboard). That is a Shopify-side action outside this repo. Once the listing is live, add the App Store install link/button to `/integrations/shopify` (currently the CTA is Book-a-Demo / Try-the-live-demo) and, if granted, a "Built for Shopify"/App-Store badge — only after Shopify actually awards it. **NEW /features OG:** `/features` currently uses the brand-placeholder `/og/default.png`; wants a real per-page product-render OG (1200×630) like the industry OGs (#14) — regenerate with `scripts/generate-og.mjs` | 7 |
| 17 | **RESOLVED (2026-08-03) — blog edge 404 fixed via Blog/CMS plugin install + 1:1 migration.** The Blog/CMS plugin is now installed on the Thridify project and the 37 WordPress posts were migrated 1:1 at matching slugs. `/blog` returns **200 on BOTH hosts** (direct Worker `site-thridify.snowy-cherry-cd2c.workers.dev` AND platform-dispatched `team-website-moj7aqqd.sites.tyashin.com`) — the platform edge's `blog-index` page (`handleBlogIndex`) now serves the installed plugin's index instead of 404ing. Individual posts serve live at `/blog/<slug>` (e.g. `/blog/5-signals-that-3d-commerce-has-crossed-the-tipping-point-in-2026` → 200). The blog is served by the platform edge (blog plugin), NOT the site's Next Worker; the old `/post/*` URLs are NOT platform-intercepted, so they fall through to the site Worker where `next.config.ts` `redirects()` 301 them (`/post/<slug>` → `/blog/<slug>` slug-preserved; tag/author/category archives → `/blog`). **Blog migration is DONE.** Residual flag: the 8 education/custom-domain help posts among the 37 logically belong to WonderlyAR (deferred per user) and remain in the blog for now. | done — verify /blog 200 on prod host at cutover |
